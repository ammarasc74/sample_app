import React from 'react';
import {Layout, Box, Text} from '../../Components';

const Settings = () => {
  return (
    <Layout>
      <Box flex={1}>
        <Text variant={'header'}>Settings Screen</Text>
      </Box>
    </Layout>
  );
};

export default Settings;
