import React from 'react';
import {Layout, Box, Text} from '../../Components';

const Home = () => {
  return (
    <Layout>
      <Box flex={1}>
        <Text variant={'header'}>عربي</Text>
      </Box>
    </Layout>
  );
};

export default Home;
