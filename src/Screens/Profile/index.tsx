import React from 'react';
import {Layout, Box, Text} from '../../Components';

const Profile = () => {
  return (
    <Layout>
      <Box flex={1}>
        <Text variant={'header'}>Profile Screen</Text>
      </Box>
    </Layout>
  );
};

export default Profile;
